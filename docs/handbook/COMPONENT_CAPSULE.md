# Component Capsule

A capsule is one bounded component plus the minimum contracts required to compare a replacement without importing the whole platform.

Required evidence: source commit, imported paths, fixtures, observed outputs, intentional differences, fault cases, independent review, shadow plan, rollback plan.
