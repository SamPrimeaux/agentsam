{
  "name": "@inneranimalmedia/agentsam-core-lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "check": "npm test && npm run validate:manifests",
    "test": "node --test",
    "validate:manifests": "node scripts/validate-component-manifests.mjs",
    "component:new": "node scripts/create-component-capsule.mjs"
  }
}
